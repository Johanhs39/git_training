## For understanding Github 

# Git push -u origin main

# git: specifies we use a git command
# Push: Updates the remote repository from your local repository 
# -u: "Upstream" sets the local repository to track the remote repository
